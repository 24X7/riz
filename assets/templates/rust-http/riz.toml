[server]
port = 3000
host = "127.0.0.1"

[function.hello]
runtime = "rust"
# Point at the release binary. Build it first:
#   cargo build --release
handler = "./target/release/hello"
timeout_ms = 5000
concurrency = 2

[[function.hello.routes]]
path = "/hello"
method = "GET"
